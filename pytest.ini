[pytest]
addopts = -q
pythonpath = .
testpaths = tests
markers =
    unit: fast isolated tests
    integration: coupled solver and io flows
    gui: GUI-related tests (offscreen)
    physics: physical consistency checks
